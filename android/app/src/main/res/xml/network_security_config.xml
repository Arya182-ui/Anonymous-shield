<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- PRODUCTION: Remove all cleartext permissions -->
    <!-- <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config> -->
    
    <!-- HTTPS only for all domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Only trust system CAs in production -->
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
    
    <!-- Certificate pinning for critical domains (if using backend) -->
    <!--
    <domain-config>
        <domain includeSubdomains="true">api.privacyvpn.com</domain>
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>