<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- PRODUCTION: Strict HTTPS-only policy -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Only trust system Certificate Authorities -->
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
    
    <!-- Block all cleartext traffic for maximum security -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">*</domain>
    </domain-config>
    
    <!-- Certificate pinning for critical VPN infrastructure (if applicable) -->
    <!--
    Example for your own VPN servers:
    <domain-config>
        <domain includeSubdomains="true">yourprivacyvpn.com</domain>
        <pin-set expiration="2027-12-31">
            <pin digest="SHA-256">YOUR_CERTIFICATE_PIN_HERE</pin>
            <pin digest="SHA-256">YOUR_BACKUP_PIN_HERE</pin>
        </pin-set>
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </domain-config>
    -->
</network-security-config>